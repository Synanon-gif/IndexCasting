# System audit report — 2026-04-07

This report is generated by [`scripts/audit-coverage.mjs`](../scripts/audit-coverage.mjs) together with the manifest [`AUDIT_COVERAGE_MANIFEST_2026-04-07.md`](./AUDIT_COVERAGE_MANIFEST_2026-04-07.md).

## 1. Coverage proof

- **Enumerated paths:** 595 (all git-tracked files).
- **Manifest JSON:** [`AUDIT_COVERAGE_MANIFEST_2026-04-07.json`](./AUDIT_COVERAGE_MANIFEST_2026-04-07.json)

### Review status aggregation

- **partially_reviewed:** 312
- **reviewed_automated:** 276
- **reviewed_manual:** 7

## 2. Automated gates

| Gate | Result |
|---|---|
| `npm run typecheck` | PASS (exit 0) |
| `npm run lint` | PASS (exit 0) |
| `jest --ci --passWithNoTests` | PASS (exit 0) |

## 3. SQL migrations (keyword scan)

Full-text scan of all files under `supabase/migrations/*.sql`:

- **files:** 71
- **mentions SECURITY DEFINER:** 60
- **mentions row_security off:** 39
- **mentions FOR ALL:** 18
- **FOR ALL on watchlist table names (heuristic):** 5
- **policies mentioning profiles + is_admin (heuristic):** 10

### 3.1 Live DB — watchlist `FOR ALL` (Management API)

Executed against project `ispkfdqzjrfrilosoklu` (same query as prior audits): **`FOR ALL` policies on** `model_embeddings`, `model_locations`, `model_agency_territories`, `calendar_entries`, `model_minor_consent` → **0 rows** (HTTP 201). Confirms no active watchlist `FOR ALL` in `public` at audit time.

> Further SECDEF inventory: [`docs/AUDIT_REPORT_2026-04-07.md`](./AUDIT_REPORT_2026-04-07.md), [`docs/RLS_AUDIT_BACKLOG.md`](./RLS_AUDIT_BACKLOG.md).

> **Note:** The watchlist `FOR ALL` count is a **heuristic** (same file mentions `FOR ALL` and `ON public.<watchlist_table>`) and may include fix migrations or comments — triage before treating as active risk.

## 4. Dependency graph, exports, rules, drift

### 4.0 Internal import graph (`src/` + `lib/`)

- **Nodes:** 207 (union of files that import or are imported internally)
- **Edges:** 586 (static `import` / `export from` / dynamic `import()` to repo files)
- **Full adjacency:** [`docs/AUDIT_DEPENDENCY_GRAPH.json`](./AUDIT_DEPENDENCY_GRAPH.json).

### 4.1 Export inventory & internal usage

- **Exported symbols (functions/classes/consts via AST):** 654
- **Symbols with ≥1 internal importer (named imports):** 435
- **Detail:** [`docs/AUDIT_EXPORT_MAP.json`](./AUDIT_EXPORT_MAP.json) (symbols + importers).

### 4.2 Rule engine: `supabase.from(`

| Classification | Count | Rule ID |
|---:|---:|---|
| allowed (service/lib) | 28 | `supabase_from_service_layer` |
| exception (AuthContext) | 2 | `supabase_from_auth_context` |
| forbidden | 0 | `supabase_from_outside_service_layer` |

No **forbidden** hits.

### 4.2b Storage `supabase.storage.from(…).upload(` — matrix allowlist

Only the service files listed in [`.cursor/rules/upload-consent-matrix.mdc`](../.cursor/rules/upload-consent-matrix.mdc) may perform **upload** chains (`from` → `upload`). Other storage calls (signed URL, list, remove) are ignored by this rule.

No violations.

### 4.3 Drift vs `docs/AUDIT_DRIFT_BASELINE.json`

- **Added files:** 0
- **Removed files:** 0
- **Modified critical files:** 0
- **New forbidden `supabase.from` sites:** 0
- **Resolved forbidden sites:** 0
- **Drift manifest fingerprint (excludes generated `docs/AUDIT_*` / dated reports):** prev `f80e09b1a83cde1f1d4a52e1d9e4dff3d2f787bc5de4800e3504d4ae0835e06a` → curr `37052a05c655071e52fe05b388eb4a8d63ee5ac2bffca3ea21352c2b323aa04a`

Update baseline after intentional changes: `AUDIT_WRITE_BASELINE=1 npm run audit:coverage`. Optional CI gate: `AUDIT_FAIL_ON_DRIFT=1`.

### 4.4 `void …catch(` (Option-A dead-code risk; exclude tests)

No production-path hits (tests may still contain patterns).

### 4.5 Snapshot-style optimistic rollback (heuristic)

No heuristic hits.

## 5. Edge Functions — manual review (7/7)

| Function | AuthN | Secrets / trust | Scope guard |
|---|---|---|---|
| **send-invite** | JWT via anon client + `Authorization` header | `RESEND_API_KEY` server-only | `get_my_org_context` RPC; optional `organization_id` must match caller membership (403 `not_member_of_organization`); multi-org warns + oldest row fallback |
| **delete-user** | JWT via anon client | `SUPABASE_SERVICE_ROLE_KEY` for `auth.admin` + storage cleanup | Self-delete OR `get_own_admin_flags` RPC (not raw `profiles.is_admin`) for cross-user delete |
| **create-checkout-session** | JWT via anon client | `STRIPE_SECRET_KEY`, service role for org resolution | `org_id` validated as caller owner; redirect URLs allowlisted (HTTPS + origin list, VULN-02) |
| **serve-watermarked-image** | JWT passed to client created with **service role** + user JWT | Service role for `can_access_platform` + signed URL | Bucket allowlist `documentspictures`; path must resolve to real `models.id` (IDOR hardening) |
| **member-remove** | JWT via anon client | Service role for admin session revoke | Caller must be **owner** of `organizationId` via `organization_members`; no self-remove |
| **send-push-notification** | `x-webhook-secret` timing-safe compare | `WEBHOOK_SECRET`, service role for DB | Only `INSERT` on `public.notifications`; `ALLOWED_NOTIFICATION_TYPES` allowlist |
| **stripe-webhook** | Stripe signature (`STRIPE_WEBHOOK_SECRET`) | Service role + `STRIPE_SECRET_KEY` | No CORS (server-to-server); subscription/org linking guard (`checkSubscriptionLinking`) |

Deploy flags: several functions use `--no-verify-jwt` at the edge; **in-function** JWT or webhook verification replaces the platform JWT gate — see each file’s header comments.

## 6. Governance: rule conflict (snapshots)

[`.cursor/rules/system-invariants.mdc`](../.cursor/rules/system-invariants.mdc) documents a **snapshot rollback** for `addModelToProject` as a special case, while [`.cursorrules`](../.cursorrules) / [`auto-review.mdc`](../.cursor/rules/auto-review.mdc) generally **forbid** snapshot-based optimistic rollback in favor of inverse operations.

**Recommendation:** Pick one canonical rule — either carve an explicit exception in global rules with justification (concurrent add-to-same-project semantics) or refactor `ClientWebApp` to inverse-operation rollback with per-id inflight locks. No rule file was changed in this audit run.

## 7. Findings summary (automated)

- **LOW** (Security): Heuristic: 5 migration file(s) mention both FOR ALL and a watchlist table name — may include fix/comment-only files; confirm live pg_policies (see RLS_AUDIT_BACKLOG).

## 8. Incremental recommendations

1. CI runs `npm run audit:coverage` with `AUDIT_FAIL_ON_DRIFT=1` and `AUDIT_CI_ENFORCE=1` (see [`docs/CI_AUDIT_AND_BASELINE.md`](./CI_AUDIT_AND_BASELINE.md)).
2. Resolve snapshot vs inverse-operation governance and update code or rules once product decides.
3. Update [`docs/LIVE_DB_WATCHLIST_SNAPSHOT.md`](./LIVE_DB_WATCHLIST_SNAPSHOT.md) after material schema/policy migrations, then re-run `npm run audit:coverage`.

