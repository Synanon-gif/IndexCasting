# Audit drift triage — May 2026

**Purpose:** Human-readable summary for CI failure on `npm run audit:coverage` (drift vs `docs/AUDIT_DRIFT_BASELINE.json`). This drift is **already present on `origin/main`**; it is **not** introduced by PR #13 (E2E-only scope).

## Baseline reference

- **File:** `docs/AUDIT_DRIFT_BASELINE.json`
- **Written at (ISO):** `2026-04-07T12:48:43.658Z`
- **`forbidden_supabase_from` in baseline:** `[]` (empty → any current forbidden sites count as “introduced” vs baseline)

## Audit command (CI-parity, drift gate)

```bash
AUDIT_FAIL_ON_DRIFT=1 AUDIT_CI_ENFORCE=1 \
  EXPO_PUBLIC_SUPABASE_URL='https://placeholder.supabase.co' \
  EXPO_PUBLIC_SUPABASE_ANON_KEY='placeholder' \
  npm run audit:coverage
```

**Expected:** non-zero exit when drift/forbidden introduce rules fail (see `docs/CI_AUDIT_AND_BASELINE.md`).

## Drift counts (representative run)

From `docs/SYSTEM_AUDIT_REPORT_<date>.md` generated by the same command family:

| Metric | Value |
|--------|------:|
| Modified critical files | **78** |
| New forbidden `supabase.from` sites | **3** |
| Resolved forbidden sites | **0** |

Full rule breakdown and critical-path list: see the dated `docs/SYSTEM_AUDIT_REPORT_*.md` next to this file after a local audit run.

## Forbidden `supabase.from` paths (rule: `supabase_from_outside_service_layer`)

| Path |
|------|
| `src/screens/InviteHealthCheckScreen.tsx:227` |
| `src/screens/InviteHealthCheckScreen.tsx:232` |
| `src/utils/notificationBatcher.ts:41` |

**Note:** Auditor message is “prefer service-layer RPC/query helpers for org-scoped access.” Review these calls before deciding to freeze state in a new baseline.

## Options

| Option | Description |
|--------|-------------|
| **A** | **Baseline refresh** after explicit review: run `AUDIT_WRITE_BASELINE=1 npm run audit:coverage` (per `CI_AUDIT_AND_BASELINE.md`), commit **only** approved artifacts (typically the updated `AUDIT_DRIFT_BASELINE.json` and policy-approved docs). |
| **B** | **Product/security refactor:** move the three call sites behind existing or new `src/services/*` helpers (or RPCs), then refresh baseline if still needed. |
| **C** | **Policy exception** only with **owner/security approval** (change audit rules or classifications) — high bar; avoid “loosen rules” without review. |

## Recommendation

1. **Review** the three `supabase.from` usages (intent, RLS, tenancy) **before** freezing baseline **A**.
2. Prefer **B** if the calls are easy to route through services without behavior change.
3. Use **A** only after stakeholders accept that CI will “green” on the current architecture for critical-path drift and forbidden list.

## Safety

- No secrets or env values belong in this document.
- Do **not** commit generated `SYSTEM_AUDIT_REPORT_*`, manifests, or graph JSON unless team policy requires versioning them; many repos commit **only** `AUDIT_DRIFT_BASELINE.json` after `AUDIT_WRITE_BASELINE=1`.

## Status

- **Baseline updated in this triage:** no (by design).
- **Product code changed:** no (this doc only).
