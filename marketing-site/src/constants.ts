/** Product web app — sign-in and authenticated flows stay on this origin. */
export const APP_ORIGIN = 'https://www.index-casting.com';

/** Marketing / landing deployment host (canonical for this Vite site). */
export const LANDING_ORIGIN = 'https://web.index-casting.com';

/** Founder-led early access (mailto only on marketing site). */
export const EARLY_ACCESS_EMAIL = 'ruben@index-casting.com';
