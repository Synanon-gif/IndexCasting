/**
 * Layout tokens for the public model Apply form (`ApplyFormView`).
 * Empty slots use a fixed height so a 3:4 aspect ratio does not scale with viewport width
 * (which made empty "+ Photo" areas nearly full-screen on web).
 */
export const APPLY_FORM_EMPTY_PHOTO_SLOT_HEIGHT = 152;

/** Portrait preview slots when an image is present (unchanged product ratio). */
export const APPLY_FORM_FILLED_PHOTO_ASPECT_RATIO = 3 / 4;
